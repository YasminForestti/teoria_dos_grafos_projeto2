#include <iostream>
#include <stdio.h>
#include <bits/stdc++.h>
#include <time.h>
#include "biblioteca.cpp"
using namespace std;

class Grafo: protected biblioteca
    {
     public:
        Grafo(int,int,float);

        void Insert(){
            int u,v;
            float p;
            
            
        }      
    };

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int numVertices,u,v;
    float peso;
    cin >> numVertices;

    return 0;
}